From: SJ Park <sj@kernel.org>
Date: Thu, 24 Sep 2026 12:22:30 -0700
Subject: [PATCH] Revert
 "mm-memcontrol-drop-kmemcg_id-and-use-mem_cgroup_id-for-list_lru-indexing-fix"

This reverts commit e38f3c422e81170a7e40e6ac0ca52e73d4a0455c.

Signed-off-by: SJ Park <sj@kernel.org>
---
 mm/memcontrol.c | 2 ++
 1 file changed, 2 insertions(+)

diff --git a/mm/memcontrol.c b/mm/memcontrol.c
index fcba9eb55659..5d7a26c91610 100644
--- a/mm/memcontrol.c
+++ b/mm/memcontrol.c
@@ -5886,6 +5886,8 @@ static void __init memcg_struct_check(void)
 				      private_id_objcg);
 	CACHELINE_ASSERT_GROUP_MEMBER(struct mem_cgroup, memcg_read_mostly,
 				      private_id);
+	CACHELINE_ASSERT_GROUP_MEMBER(struct mem_cgroup, memcg_read_mostly,
+				      kmemcg_id);
 	CACHELINE_ASSERT_GROUP_MEMBER(struct mem_cgroup, memcg_read_mostly,
 				      oom_group);
 
-- 
2.47.3

