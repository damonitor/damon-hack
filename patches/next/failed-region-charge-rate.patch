From: SeongJae Park <sj@kernel.org>
Date: Fri, 6 Mar 2026 17:07:53 -0800
Subject: [PATCH] ==== failed region charge rate ====

mm/damon: introduce DAMOS failed region quota charge ratio

TL; DR: Let users set different DAMOS quota charge ratio for DAMOS
action failed regions, for deterministic and consistent DAMOS action
progress.

One common issue we get reported from DAMON users is that DAMOS action
applying progress speed is sometimes get much slower than expected.  And
one commit root cause is that the DAMOS quota is exceeded by the action
applying failed memory regions.

Case Study: Unexpectedly Slow DAMON_RECLAIM
===========================================

For example, a group of users tried to run DAMOS-based proactive memory
reclamation (DAMON_RECLAIM) with 100 MiB per second DAMOS quota.  They
ran it on a system having no active workload which means all memory
ofthe system is cold.  The expectation was that the system will show 100
MiB per second reclamation until (nearly) all memory is reclaimed.  But
what they found is that the speed is inconsistent under the 100 MiB per
second speed, and sometimes it becomes very slower than their hope.

By showing the number of DAMOS quota exceeds in real time via DAMOS
stat, we found DAMOS quota is always exceeded when the speed is slow.
By showing the amount of DAMOS action (reclamation in this case) failed
regions in realtime, again via DAMOS stat, we found the reclamation
attempts were nearly always failed when the speed is slow.

DAMOS quota samely charges DAMOS action tried regions regardless of the
successfulness of the try.  Hence in the example reported case, there
were unreclaimable memory spread around the system memory.  Sometimes
nearly 100 MiB of memory that DAMOS was tried to reclaim in the given
quota interval was reclaimable, and therefore showed nearly 100 MiB per
second speed.  Sometimes nearly 99 MiB of memory that DAMOS was tried to
reclaim in the given quota interval was unreclaimable, and therefore
showing only about 1 MiB per reclaim speed.

We explained it is an expected behavior of the feature rather than a
bug, as DAMOS quota is there for only upper-limit of the speed.  The
users agreed and later reported a huge win from the adoption of
DAMON_RECLAIM on their products.

It is a Feature, But...
=======================

So nothing is broken.  DAMOS quota is working as intended, as the upper
limit of the speed.  It also provides its behavior observability via
DAMOS stat.  On the real world production environment that runs long
term workloads and stability matters, sometimes slow speed is no real
problem.

But, the non-deterministic behavior is sometimes annoying, especially on
lab environment.  Even on realistic production environment, when there
are huge amount of unreclaimable memory, the speed could be
problematically slow.  Let's suppose a virtual machines provider that
utilizes a system having multiple TiB of memory but providing 99% of the
memory to virtual machines as hugetlb pages that cannot be reclaimed.
When aim-oriented DAMOS auto-tuning is applied, this could also make the
internal feedback loop confused.

The intention of the current behavior was that trying DAMOS action to
regions would anyway impose some overhead, and therefore somehow be
charged.  But in the real world, the overhead for failed action
is much lighter than succeeded action.  Charging those in same rate may
unfair, or at least suboptimum in some environments.

DAMOS Action Failed Region Quota Charge Rate
============================================

Let users set the charge ratio for the action-failed memory, for more
optimal and deterministic use of DAMOS.  It allows users to specify the
numerator and the denominator of the ratio for flexible setup.  For
example, let's suppose the numerator and the denominator are set to 1
and 4,096, respectively.  The ratio is 1 / 4,096.  A DAMOS scheme action
is applied to 5 GiB memory.  For 1 GiB of the memory, the action is
succeeded.  For the rest (4 GiB), the action is failed.  Then, only 1
GiB and 1 MiB quota is charged.

The optimal charge ratio will depend on the use case and
system/workload.  I believe nominator of value 1 and denominator of
value PAGE_SIZE can be a good starting point, though.

Tests
=====

I tested this feature in below steps.

1. Allocate 50% of system memory and mlock() it using a test program.
2. Fill up page cache to exhaust nearly all free memory.
3. Start DAMON-based proactive reclamation with 100 MiB/second
   hard-quota.  Auto-tune the soft-quota under the hard-quota for
   achieving 40% free memory of the system with 'temporal' tuner.

For the step 1, I used a simple C program that written by Gemini.  It is
too simple, so I'm not sharing the code here.

For the step 2, I use dd command like below:

   dd if=/dev/zero of=foo bs=1M count=$50_percent_of_system_memory

For the step 3, I use the latest version of DAMON user-space tool (damo)
like below.

    sudo ./damo start --damos_action pageout \
            ` # Do the pageout only up to 100 MiB per second `
            --damos_quota_space 100M --damos_quota_interval 1s \
            ` # Auto-tune the quota below the hard quota aiming` \
            ` # 40% free memory of the node 0 ` \
            ` # (entire node of the test system)` \
            --damos_quota_goal node_mem_free_bp 40% 0 \
            ` # use temporal tuner, which is easy to understnd ` \
            --damos_quota_goal_tuner temporal

As expected, the progress of the reclamation is not consistent, because
the quota is exceeded for the failed reclamation of the unreclamable memory.

I do this again, but with failed region charge ratio feature.  For this,
the above 'damo' command is used, after appending below command line option.
Note that the option was added to 'damo' after v3.1.9.

    sudo ./damo start --damos_action pageout \
            [...]
            ` # quota-charge only 1/4096 for pageout-failed regions ` \
            --damos_quota_fail_charge_ratio 1 4096

The progress of the reclamation was nearly 100 MiB per second until the
goal is achieved, met the expectation.

Signed-off-by: SeongJae Park <sj@kernel.org>
---
 hkml_cv_bogus/hkml_cv_bogus_yi9absiw | 0
 1 file changed, 0 insertions(+), 0 deletions(-)
 create mode 100644 hkml_cv_bogus/hkml_cv_bogus_yi9absiw

diff --git a/hkml_cv_bogus/hkml_cv_bogus_yi9absiw b/hkml_cv_bogus/hkml_cv_bogus_yi9absiw
new file mode 100644
index 000000000000..e69de29bb2d1
-- 
2.47.3

