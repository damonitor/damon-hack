From: SeongJae Park <sj@kernel.org>
Date: Sun, 19 Apr 2026 12:06:59 -0700
Subject: [PATCH] mm/damon/core: move data_attr from damon_attrs to damon_ctx

Signed-off-by: SeongJae Park <sj@kernel.org>
---
 include/linux/damon.h |  6 +++---
 mm/damon/core.c       | 10 +++++-----
 2 files changed, 8 insertions(+), 8 deletions(-)

diff --git a/include/linux/damon.h b/include/linux/damon.h
index 01d39203c1472..54a4d1de220f4 100644
--- a/include/linux/damon.h
+++ b/include/linux/damon.h
@@ -854,7 +854,6 @@ struct damon_attrs {
 	unsigned long aggr_interval;
 	unsigned long ops_update_interval;
 	struct damon_intervals_goal intervals_goal;
-	struct list_head data_attrs;
 	unsigned long min_nr_regions;
 	unsigned long max_nr_regions;
 /* private: internal use only */
@@ -1022,6 +1021,7 @@ struct damon_ctx {
 
 /* public: */
 	struct damon_operations ops;
+	struct list_head data_attrs;
 	struct damon_sample_control sample_control;
 	unsigned long addr_unit;
 	unsigned long min_region_sz;
@@ -1057,7 +1057,7 @@ static inline unsigned long damon_sz_region(struct damon_region *r)
 }
 
 #define damon_for_each_data_attr(a, ctx) \
-	list_for_each_entry(a, &ctx->attrs.data_attrs, list)
+	list_for_each_entry(a, &ctx->data_attrs, list)
 
 #define damon_for_each_region(r, t) \
 	list_for_each_entry(r, &t->regions_list, list)
@@ -1112,7 +1112,7 @@ void damon_add_filter(struct damon_data_attr *attr, struct damon_filter *f);
 void damon_destroy_filter(struct damon_filter *f);
 
 struct damon_data_attr *damon_new_data_attr(void);
-void damon_add_data_attr(struct damon_attrs *attrs, struct damon_data_attr *a);
+void damon_add_data_attr(struct damon_ctx *c, struct damon_data_attr *a);
 void damon_destroy_data_attr(struct damon_data_attr *a);
 
 struct damon_region *damon_new_region(unsigned long start, unsigned long end);
diff --git a/mm/damon/core.c b/mm/damon/core.c
index e2b8974f4c11a..74dc8eedbb9a1 100644
--- a/mm/damon/core.c
+++ b/mm/damon/core.c
@@ -164,11 +164,9 @@ struct damon_data_attr *damon_new_data_attr(void)
 	return a;
 }
 
-void damon_add_data_attr(struct damon_attrs *attrs, struct damon_data_attr *a)
+void damon_add_data_attr(struct damon_ctx *c, struct damon_data_attr *a)
 {
-	if (!attrs->data_attrs.prev)
-		INIT_LIST_HEAD(&attrs->data_attrs);
-	list_add_tail(&a->list, &attrs->data_attrs);
+	list_add_tail(&a->list, &c->data_attrs);
 }
 
 static void damon_del_data_attr(struct damon_data_attr *a)
@@ -783,6 +781,8 @@ struct damon_ctx *damon_new_ctx(void)
 	ctx->attrs.min_nr_regions = 10;
 	ctx->attrs.max_nr_regions = 1000;
 
+	INIT_LIST_HEAD(&ctx->data_attrs);
+
 	ctx->sample_control.primitives_enabled.page_table = true;
 	INIT_LIST_HEAD(&ctx->sample_control.sample_filters);
 
@@ -3547,7 +3547,7 @@ static int kdamond_fn(void *data)
 			max_nr_accesses = kdamond_check_reported_accesses(ctx);
 		else if (ctx->ops.check_accesses)
 			max_nr_accesses = ctx->ops.check_accesses(ctx);
-		if (ctx->ops.check_data_attrs && ctx->attrs.data_attrs.prev)
+		if (ctx->ops.check_data_attrs)
 			ctx->ops.check_data_attrs(ctx);
 
 		if (time_after_eq(ctx->passed_sample_intervals,
-- 
2.47.3

