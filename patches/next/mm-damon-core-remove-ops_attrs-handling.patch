From: SeongJae Park <sj@kernel.org>
Date: Sun, 30 Nov 2025 13:30:17 -0800
Subject: [PATCH] mm/damon/core: remove ops_attrs handling

It is not really be used.  Remove.

Signed-off-by: SeongJae Park <sj@kernel.org>
---
 mm/damon/core.c | 4 ----
 1 file changed, 4 deletions(-)

diff --git a/mm/damon/core.c b/mm/damon/core.c
index 2bc24153768d..3e8aebdb230a 100644
--- a/mm/damon/core.c
+++ b/mm/damon/core.c
@@ -661,9 +661,6 @@ void damon_destroy_ctx(struct damon_ctx *ctx)
 	damon_for_each_sample_filter_safe(f, next_f, &ctx->sample_control)
 		damon_destroy_sample_filter(f, &ctx->sample_control);
 
-	if (ctx->ops_attrs.nr_tids)
-		kfree(ctx->ops_attrs.tids);
-
 	kfree(ctx);
 }
 
@@ -1448,7 +1445,6 @@ int damon_commit_ctx(struct damon_ctx *dst, struct damon_ctx *src)
 			return err;
 	}
 	dst->ops = src->ops;
-	dst->ops_attrs = src->ops_attrs;
 	err = damon_commit_sample_control(&dst->sample_control,
 			&src->sample_control);
 	if (err)
-- 
2.47.3

