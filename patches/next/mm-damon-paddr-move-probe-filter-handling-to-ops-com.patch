From: SJ Park <sj@kernel.org>
Date: Thu, 6 Aug 2026 07:16:23 -0700
Subject: [PATCH] mm/damon/paddr: move probe filter handling to ops-common

It will be used in vaddr.

Signed-off-by: SJ Park <sj@kernel.org>
---
 mm/damon/ops-common.c | 54 +++++++++++++++++++++++++++++++++++++++++
 mm/damon/ops-common.h |  1 +
 mm/damon/paddr.c      | 56 +------------------------------------------
 3 files changed, 56 insertions(+), 55 deletions(-)

diff --git a/mm/damon/ops-common.c b/mm/damon/ops-common.c
index fbda70d8ea4d0..f42de93db5bc8 100644
--- a/mm/damon/ops-common.c
+++ b/mm/damon/ops-common.c
@@ -439,3 +439,57 @@ bool damos_ops_has_filter(struct damos *s)
 		return true;
 	return false;
 }
+
+static bool damon_ops_filter_match(struct damon_filter *filter,
+		struct folio *folio)
+{
+	bool matched = false;
+	struct mem_cgroup *memcg;
+
+	switch (filter->type) {
+	case DAMON_FILTER_TYPE_ANON:
+		if (!folio) {
+			matched = false;
+			break;
+		}
+		matched = folio_test_anon(folio);
+		break;
+	case DAMON_FILTER_TYPE_MEMCG:
+		if (!folio) {
+			matched = false;
+			break;
+		}
+		rcu_read_lock();
+		memcg = folio_memcg_check(folio);
+		if (!memcg)
+			matched = false;
+		else
+			matched = filter->memcg_id == mem_cgroup_id(memcg);
+		rcu_read_unlock();
+		break;
+	case DAMON_FILTER_TYPE_PGIDLE_UNSET:
+		if (!folio)
+			matched = false;
+		else
+			matched = damon_folio_young(folio);
+		break;
+	default:
+		break;
+	}
+	return matched == filter->matching;
+}
+
+bool damon_ops_filter_pass(struct folio *folio, struct damon_probe *p)
+{
+	struct damon_filter *f;
+	bool pass = true;
+
+	damon_for_each_filter(f, p) {
+		if (damon_ops_filter_match(f, folio)) {
+			pass = f->allow;
+			break;
+		}
+		pass = !f->allow;
+	}
+	return pass;
+}
diff --git a/mm/damon/ops-common.h b/mm/damon/ops-common.h
index 38d295488fa18..fa96938b9a3f8 100644
--- a/mm/damon/ops-common.h
+++ b/mm/damon/ops-common.h
@@ -21,3 +21,4 @@ bool damos_folio_filter_match(struct damos_filter *filter, struct folio *folio);
 unsigned long damon_migrate_pages(struct list_head *folio_list, int target_nid);
 
 bool damos_ops_has_filter(struct damos *s);
+bool damon_ops_filter_pass(struct folio *folio, struct damon_probe *p);
diff --git a/mm/damon/paddr.c b/mm/damon/paddr.c
index a689a41169f96..86a2da7355b09 100644
--- a/mm/damon/paddr.c
+++ b/mm/damon/paddr.c
@@ -204,60 +204,6 @@ static void damon_pa_prep_probes(struct damon_ctx *ctx, bool set_samples)
 	}
 }
 
-static bool damon_pa_filter_match(struct damon_filter *filter,
-		struct folio *folio)
-{
-	bool matched = false;
-	struct mem_cgroup *memcg;
-
-	switch (filter->type) {
-	case DAMON_FILTER_TYPE_ANON:
-		if (!folio) {
-			matched = false;
-			break;
-		}
-		matched = folio_test_anon(folio);
-		break;
-	case DAMON_FILTER_TYPE_MEMCG:
-		if (!folio) {
-			matched = false;
-			break;
-		}
-		rcu_read_lock();
-		memcg = folio_memcg_check(folio);
-		if (!memcg)
-			matched = false;
-		else
-			matched = filter->memcg_id == mem_cgroup_id(memcg);
-		rcu_read_unlock();
-		break;
-	case DAMON_FILTER_TYPE_PGIDLE_UNSET:
-		if (!folio)
-			matched = false;
-		else
-			matched = damon_folio_young(folio);
-		break;
-	default:
-		break;
-	}
-	return matched == filter->matching;
-}
-
-static bool damon_pa_filter_pass(struct folio *folio, struct damon_probe *p)
-{
-	struct damon_filter *f;
-	bool pass = true;
-
-	damon_for_each_filter(f, p) {
-		if (damon_pa_filter_match(f, folio)) {
-			pass = f->allow;
-			break;
-		}
-		pass = !f->allow;
-	}
-	return pass;
-}
-
 static unsigned int damon_pa_apply_probes(struct damon_ctx *ctx,
 		bool set_samples, bool return_max_wsum)
 {
@@ -279,7 +225,7 @@ static unsigned int damon_pa_apply_probes(struct damon_ctx *ctx,
 					ctx->addr_unit);
 			folio = damon_get_folio(PHYS_PFN(pa));
 			damon_for_each_probe(p, ctx) {
-				if (damon_pa_filter_pass(folio, p))
+				if (damon_ops_filter_pass(folio, p))
 					r->probe_hits[i]++;
 				i++;
 			}
-- 
2.47.3

