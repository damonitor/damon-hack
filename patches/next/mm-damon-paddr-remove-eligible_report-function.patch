From: SeongJae Park <sj@kernel.org>
Date: Sun, 30 Nov 2025 13:31:28 -0800
Subject: [PATCH] mm/damon/paddr: remove eligible_report function

It is not really being used.  Remove.

Signed-off-by: SeongJae Park <sj@kernel.org>
---
 mm/damon/paddr.c | 20 --------------------
 1 file changed, 20 deletions(-)

diff --git a/mm/damon/paddr.c b/mm/damon/paddr.c
index 2da102360734..1e501b2f3533 100644
--- a/mm/damon/paddr.c
+++ b/mm/damon/paddr.c
@@ -184,25 +184,6 @@ static unsigned int damon_pa_check_accesses(struct damon_ctx *ctx)
 	return max_nr_accesses;
 }
 
-static bool damon_pa_eligible_report(struct damon_access_report *report,
-		struct damon_target *t,
-		struct damon_operations_attrs *ops_attrs)
-{
-	int i;
-
-	if (ops_attrs->write_only && report->is_write)
-		return false;
-	if (!cpumask_test_cpu(report->cpu, &ops_attrs->cpus))
-		return false;
-	if (!ops_attrs->nr_tids)
-		return true;
-	for (i = 0; i < ops_attrs->nr_tids; i++) {
-		if (report->tid == ops_attrs->tids[i])
-			return true;
-	}
-	return false;
-}
-
 /*
  * damos_pa_filter_out - Return true if the page should be filtered out.
  */
@@ -548,7 +529,6 @@ static int __init damon_pa_initcall(void)
 		.update = NULL,
 		.prepare_access_checks = damon_pa_prepare_access_checks,
 		.check_accesses = damon_pa_check_accesses,
-		.eligible_report = damon_pa_eligible_report,
 		.target_valid = NULL,
 		.cleanup = NULL,
 		.apply_scheme = damon_pa_apply_scheme,
-- 
2.47.3

