From: SeongJae Park <sj@kernel.org>
Date: Sat, 12 Apr 2025 13:14:33 -0700
Subject: [PATCH] mm/damon/paddr: support target->addr_unit for
 DAMOS_MIGRATE_{HOT,COLD}

Signed-off-by: SeongJae Park <sj@kernel.org>
---
 mm/damon/paddr.c | 12 +++++++-----
 1 file changed, 7 insertions(+), 5 deletions(-)

diff --git a/mm/damon/paddr.c b/mm/damon/paddr.c
index 493db57b4668..ff48a5cc20ee 100644
--- a/mm/damon/paddr.c
+++ b/mm/damon/paddr.c
@@ -656,15 +656,17 @@ static unsigned long damon_pa_migrate_pages(struct list_head *folio_list,
 	return nr_migrated;
 }
 
-static unsigned long damon_pa_migrate(struct damon_region *r, struct damos *s,
+static unsigned long damon_pa_migrate(struct damon_region *r,
+		struct damon_target *t, struct damos *s,
 		unsigned long *sz_filter_passed)
 {
-	unsigned long addr, applied;
+	phys_addr_t addr;
+	unsigned long applied;
 	LIST_HEAD(folio_list);
 	struct folio *folio;
 
-	addr = r->ar.start;
-	while (addr < r->ar.end) {
+	addr = damon_pa_real_addr(r->ar.start, t);
+	while (addr < damon_pa_real_addr(r->ar.end, t)) {
 		folio = damon_get_folio(PHYS_PFN(addr));
 		if (damon_pa_invalid_damos_folio(folio, s)) {
 			addr += PAGE_SIZE;
@@ -826,7 +828,7 @@ static unsigned long damon_pa_apply_scheme(struct damon_ctx *ctx,
 				r, t, scheme, sz_filter_passed);
 	case DAMOS_MIGRATE_HOT:
 	case DAMOS_MIGRATE_COLD:
-		return damon_pa_migrate(r, scheme, sz_filter_passed);
+		return damon_pa_migrate(r, t, scheme, sz_filter_passed);
 #ifdef CONFIG_ACMA
 	case DAMOS_ALLOC:
 		return damon_pa_alloc_or_free(r, scheme, true);
-- 
2.39.5

