From: SeongJae Park <sj@kernel.org>
Date: Wed, 20 May 2026 22:26:43 -0700
Subject: [PATCH] mm/damon/tests/vaddr-kunit: (fixup) free target on fail path

Signed-off-by: SeongJae Park <sj@kernel.org>
---
 mm/damon/tests/vaddr-kunit.h | 5 ++++-
 1 file changed, 4 insertions(+), 1 deletion(-)

diff --git a/mm/damon/tests/vaddr-kunit.h b/mm/damon/tests/vaddr-kunit.h
index de30db9b753e5..563fbc7e3f448 100644
--- a/mm/damon/tests/vaddr-kunit.h
+++ b/mm/damon/tests/vaddr-kunit.h
@@ -152,12 +152,15 @@ static void damon_do_test_apply_three_regions(struct kunit *test,
 	if (damon_set_regions(t, ranges, nr_regions / 2,
 				DAMON_MIN_REGION_SZ)) {
 		kfree(ranges);
+		damon_destroy_target(t, NULL);
 		kunit_skip(test, "damon_set_regions() fail");
 	}
 	kfree(ranges);
 
-	if (damon_set_regions(t, three_regions, 3, DAMON_MIN_REGION_SZ))
+	if (damon_set_regions(t, three_regions, 3, DAMON_MIN_REGION_SZ)) {
+		damon_destroy_target(t, NULL);
 		kunit_skip(test, "second damon_set_regions() fail");
+	}
 
 	for (i = 0; i < nr_expected / 2; i++) {
 		r = __nth_region_of(t, i);
-- 
2.47.3

